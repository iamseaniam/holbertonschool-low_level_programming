#include "main.h"
#include <stdio.h>
/**
 * @brief This function prints a string followed by a new line.'
 *
 * @param s The string to print.
 */
void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
