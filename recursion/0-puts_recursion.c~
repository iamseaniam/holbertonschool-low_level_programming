#include "main.h"
#include <stdio.h>
/**
*_puts_recursion - This function recursively prints a string followed by a new
*@s: The string to print.
*
*Description: This function takes a pointer to a string 's' and recursively pri
*character of the string followed by a new line until the end of the string is
*/
void _puts_recursion(char *s)
{
	printf("%s\n", s);
}
