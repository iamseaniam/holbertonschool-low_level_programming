#include "main.h"

size_t print_list(const list_t *h);
{
    if (*str == NULL)
        print("[0] (nil)");

    while node:
    {
        printf(node.value)
        node = node.next
    }
    return(h)
}

